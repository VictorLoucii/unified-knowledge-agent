# docker-compose.yml
version: '3.8'

services:
  # postgres:
  #   image: postgres:15-alpine
  #   restart: unless-stopped
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: password123
  #     POSTGRES_DB: postgres
  #   ports:
  #     - "5432:5432" 
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data 
  #   # Added from my version: Ensures backend waits for DB to be fully ready
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres -d postgres"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  backend:
    build:
      context: .
      dockerfile: backend.Dockerfile
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - DATABASE_URL=postgresql://postgres.zpsguwilzanoraxwfsty:0I3cdkk8KAY6803O@aws-1-ap-south-1.pooler.supabase.com:5432/postgres
    volumes:
      # Kept from your version: CRITICAL for keeping your AI's knowledge base!
      - ./chroma_db:/app/chroma_db
      - ./docstore:/app/docstore
    # depends_on:
    #   postgres:
    #     condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: frontend.Dockerfile
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend

volumes:
  postgres_data: